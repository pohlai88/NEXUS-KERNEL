'use client';

/**
 * Providers Component
 * 
 * Nexus Design System handles all theming and styling.
 * No external UI providers needed.
 */
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
